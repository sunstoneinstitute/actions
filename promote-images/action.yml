name: Promote Images
description: >
  Promote container images from one environment to another. Resolves the
  source deploy branch commit, computes a semver prod tag, copies images
  with crane, updates the target deploy branch, and creates a git tag.
  Caller must: checkout with fetch-depth 0, authenticate to both registries,
  and have crane available (or let this action install it).

inputs:
  from-env:
    description: Source environment
    required: false
    default: dev
  to-env:
    description: Target environment
    required: false
    default: prod
  images:
    description: >
      Space-separated list of image names (e.g. "hugin molnir" or
      "app migrations"). Must match the image name suffixes in overlay
      kustomization.yaml files.
    required: true
  from-registry:
    description: Source container registry prefix
    required: true
  to-registry:
    description: Target container registry prefix
    required: true
  version-file:
    description: >
      Path to the file containing the version (.toml or .json).
      If omitted, auto-detects pyproject.toml or package.json in repo root.
    required: false
  bump:
    description: >
      Version bump type (patch, minor, major), passed through to
      compute-version. When set, the target version is computed from
      existing git tags and version-file is ignored.
    required: false
  source-sha:
    description: >
      Specific commit SHA on the source deploy branch to promote.
      Defaults to the latest commit on last-deploy/<from-env>.
    required: false
  overlay-base:
    description: Base path for Kustomize overlays
    required: false
    default: deploy/overlays
  overlay-path:
    description: >
      Glob pattern for overlay kustomization files in the TARGET env, passed
      through to update-deploy-branch. When omitted, auto-detected from
      overlay-base: tries per-image subdirs first, falls back to flat layout.
    required: false
  from-overlay-path:
    description: >
      Path to the single kustomization.yaml in the SOURCE env to read the
      current image tag from. Required when the source overlay directory is
      not named after from-env (e.g. env "dev" living in
      deploy/overlays/hzdev). When omitted, auto-detected from overlay-base
      and from-env: tries the per-image subdir first, falls back to flat.
    required: false

outputs:
  from-tag:
    description: Image tag on the source environment
    value: ${{ steps.resolve.outputs.tag }}
  to-tag:
    description: Semver tag applied to the target environment
    value: ${{ steps.version.outputs.tag }}
  source-sha:
    description: Commit SHA on the source deploy branch that was promoted
    value: ${{ steps.resolve.outputs.sha }}

runs:
  using: composite
  steps:
    - name: Install crane
      uses: imjasonh/setup-crane@v0.5

    - name: Install yq
      uses: mikefarah/yq@v4

    - name: Resolve source deploy commit
      id: resolve
      shell: bash
      run: |
        set -euo pipefail
        FROM_ENV="${{ inputs.from-env }}"
        SOURCE_SHA="${{ inputs.source-sha }}"
        IMAGES="${{ inputs.images }}"
        OVERLAY_BASE="${{ inputs.overlay-base }}"
        BRANCH="last-deploy/${FROM_ENV}"

        git fetch origin "$BRANCH"

        # Resolve the main SHA that the source deploy branch is based on
        # by reading the main-sha trailer from the latest deploy commit.
        # NB: materialise the log first. Piping git straight into an
        # early-exiting filter (grep -m1, head -1) kills git with SIGPIPE,
        # which `pipefail` reports as exit 141.
        if [ -z "$SOURCE_SHA" ]; then
          DEPLOY_LOG=$(git log --format=%B -20 "origin/${BRANCH}")
          SOURCE_SHA=$(awk 'index($0, "main-sha:") == 1 { print $2; exit }' <<<"$DEPLOY_LOG")
          if [ -z "$SOURCE_SHA" ]; then
            echo "::error::No main-sha trailer found on ${BRANCH}. Deploy branch needs manual repair."
            exit 1
          fi
        fi
        echo "sha=${SOURCE_SHA}" >> "$GITHUB_OUTPUT"

        # Extract the image tag from the source overlay kustomization.
        # Explicit from-overlay-path wins; otherwise try the per-image subdir
        # layout, then the flat layout. Auto-detection assumes the overlay dir
        # is named after from-env, which is not true everywhere — set
        # from-overlay-path when it isn't.
        FIRST_IMAGE=$(echo "$IMAGES" | awk '{print $1}')
        KUSTOMIZATION="${{ inputs.from-overlay-path }}"
        if [ -z "$KUSTOMIZATION" ]; then
          SUBDIR_KUSTOMIZATION="${OVERLAY_BASE}/${FROM_ENV}/${FIRST_IMAGE}/kustomization.yaml"
          FLAT_KUSTOMIZATION="${OVERLAY_BASE}/${FROM_ENV}/kustomization.yaml"

          if git show "origin/${BRANCH}:${SUBDIR_KUSTOMIZATION}" >/dev/null 2>&1; then
            KUSTOMIZATION="$SUBDIR_KUSTOMIZATION"
          else
            KUSTOMIZATION="$FLAT_KUSTOMIZATION"
          fi
        fi

        if ! git show "origin/${BRANCH}:${KUSTOMIZATION}" >/dev/null 2>&1; then
          echo "::error::${KUSTOMIZATION} does not exist on ${BRANCH}. Set from-overlay-path if the source overlay directory is not named '${FROM_ENV}'."
          exit 1
        fi

        git checkout "origin/${BRANCH}" -- "$KUSTOMIZATION"
        TAG=$(yq ".images[] | select(.name == \"*${FIRST_IMAGE}\") | .newTag" "$KUSTOMIZATION")
        # Fall back to first image entry if name selector didn't match
        if [ -z "$TAG" ] || [ "$TAG" = "null" ]; then
          TAG=$(yq '.images[0].newTag' "$KUSTOMIZATION")
        fi
        git checkout HEAD -- .

        if [ -z "$TAG" ] || [ "$TAG" = "null" ]; then
          echo "::error::Could not extract image tag from ${KUSTOMIZATION}"
          exit 1
        fi

        echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
        echo "Source: ${BRANCH} @ ${SOURCE_SHA::7}, tag: ${TAG}"

    - name: Compute target version
      id: version
      uses: sunstoneinstitute/actions/compute-version@v1
      with:
        version-file: ${{ inputs.version-file }}
        bump: ${{ inputs.bump }}

    - name: Copy images to target registry
      shell: bash
      run: |
        set -euo pipefail
        FROM_TAG="${{ steps.resolve.outputs.tag }}"
        TO_TAG="${{ steps.version.outputs.tag }}"
        FROM_REGISTRY="${{ inputs.from-registry }}"
        TO_REGISTRY="${{ inputs.to-registry }}"

        for img in ${{ inputs.images }}; do
          echo "Copying ${img}:${FROM_TAG} → ${img}:${TO_TAG}"
          crane copy \
            "${FROM_REGISTRY}/${img}:${FROM_TAG}" \
            "${TO_REGISTRY}/${img}:${TO_TAG}"
        done

    - name: Detect overlay path for target env
      id: overlay
      shell: bash
      run: |
        OVERLAY_PATH="${{ inputs.overlay-path }}"
        if [ -z "$OVERLAY_PATH" ]; then
          TO_ENV="${{ inputs.to-env }}"
          OVERLAY_BASE="${{ inputs.overlay-base }}"
          FLAT="${OVERLAY_BASE}/${TO_ENV}/kustomization.yaml"

          # Prefer the flat layout when it has an images: section. Otherwise
          # check if any per-image subdir kustomization declares images: —
          # subdirs without images: (e.g. a migrations Job overlay) are NOT
          # per-image overlays and must not drive the auto-detection.
          if [ -f "$FLAT" ] && yq -e '.images' "$FLAT" >/dev/null 2>&1; then
            OVERLAY_PATH="$FLAT"
          else
            FOUND_IMAGE_SUBDIR=false
            for f in "${OVERLAY_BASE}/${TO_ENV}"/*/kustomization.yaml; do
              [ -f "$f" ] || continue
              if yq -e '.images' "$f" >/dev/null 2>&1; then
                FOUND_IMAGE_SUBDIR=true
                break
              fi
            done
            if [ "$FOUND_IMAGE_SUBDIR" = "true" ]; then
              OVERLAY_PATH="${OVERLAY_BASE}/${TO_ENV}/*/kustomization.yaml"
            else
              # Default to flat even if it lacks images:; downstream
              # update-deploy-branch will surface a clear error.
              OVERLAY_PATH="$FLAT"
            fi
          fi
        fi
        echo "path=${OVERLAY_PATH}" >> "$GITHUB_OUTPUT"

    - name: Update target deploy branch
      uses: sunstoneinstitute/actions/update-deploy-branch@v1
      with:
        env: ${{ inputs.to-env }}
        images: ${{ inputs.images }}
        registry: ${{ inputs.to-registry }}
        tag: ${{ steps.version.outputs.tag }}
        source-ref: ${{ steps.resolve.outputs.sha }}
        overlay-path: ${{ steps.overlay.outputs.path }}

    - name: Create git tag
      shell: bash
      run: |
        TAG="${{ steps.version.outputs.tag }}"
        git tag "$TAG"
        git push origin "$TAG"
        echo "Tagged ${TAG}"
